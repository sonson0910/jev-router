{
  "description": "Optional Jev routing hint with fail-open fallback for Codex and Claude Code.",
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT:-$PLUGIN_ROOT}/hooks/jev-router.cjs\"",
            "timeout": 3,
            "statusMessage": "Asking Jev for a routing hint"
          }
        ]
      }
    ]
  }
}
